enum Path {
  ROOT = '/',
  APP = '/app',
  AUTH = '/auth',
  NOT_FOUND = '/404',
  NO_ACCESS = '/no-access',
  ALL = '*',
}

export enum AppPath {
  Home = 'home',
}

export default Path;
